# URL of your deployed backend (no trailing slash).
# Leave empty in local dev — Vite proxy handles it automatically.
VITE_API_URL=https://your-backend.onrender.com

# Set to /your-repo-name/ when deploying to GitHub Pages.
# Leave empty or / for local dev and custom domains.
VITE_BASE_PATH=/soccer-fifa-ratings/
